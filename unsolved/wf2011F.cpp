#include <bits/stdc++.h>
using namespace std;
const int N=100005; 
int main(){
	int n,t[N],r[N],p[N],b,t,dp[N];
	cin>>n;
	for(int i=0;i<n;i++)
		cin>>t[i]>>r[i]>>p[i];
	cin>>b;
	for(int i=0;i<n;i++){
		
		dp[i]=;
	}
}
