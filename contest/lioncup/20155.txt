int n = int::Parse(textBox1->Text);
		int mia = 1000000, i, mi = 0;
		for (int i = 0;; i++){
			int ans = 0;
			for (int j = 1; j <= n; j++)
				ans += j*abs(i-j);
			if (ans > mia)break;
			mia = ans; mi = i;
		}

		for (int i = -1;; i--){
			int ans = 0;
			for (int j = 1; j <= n; j++)
				ans += j*abs(i - j);
			if (ans > mia)break;
			mia = ans; mi = i;
		}
		textBox2->Text = mia.ToString();